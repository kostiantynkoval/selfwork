import { ShoworhidePipe } from './showorhide.pipe';

describe('ShoworhidePipe', () => {
  it('create an instance', () => {
    const pipe = new ShoworhidePipe();
    expect(pipe).toBeTruthy();
  });
});
